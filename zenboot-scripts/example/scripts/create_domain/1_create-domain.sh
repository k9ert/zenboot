#!/bin/bash

#@Scriptlet(author="Tobias Schuhmacher (tschuhmacher@nemeses.de)", description="Create a host instance (FAKE!)")
#@Parameters([
#  @Parameter(name="DOMAIN",   type=ParameterType.CONSUME, description="The domain which need to be created"),
#  @Parameter(name="DOMAIN",   type=ParameterType.PUBLISH, description="The domain which need to be created"),
#])

echo -n "some heavy lifting here configuring a domainserver ..."
echo "done"

echo "DOMAIN=$DOMAIN"
