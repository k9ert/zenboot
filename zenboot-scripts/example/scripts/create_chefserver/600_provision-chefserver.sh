#!/bin/bash

#@Scriptlet(author="Kim Neunert (kim.neunert@hybris.com)", description="fakeprovisions a chefserver")
#@Parameters([
#  @Parameter(name="FQDN",     type=ParameterType.CONSUME),
#])


echo -n "# provision the chefserver at $FQDN ..."

echo "# done"
